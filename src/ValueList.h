#ifndef __ListPart_h__
#define __ListPart_h__

const char ELEMENT_SEPARATOR = ';';	// we are on Windows, right? ;)

#define ENUM_CLASS
enum ENUM_CLASS MATCH_TYPE { NORMAL, PATH };

typedef string::size_type	CHARPOS;

#endif //__ListPart_h__
